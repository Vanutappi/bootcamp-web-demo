@model IEnumerable<WorkhourDemo.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Yksiköt</h2>

<p>
    @Html.ActionLink("Lisää Uusi", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <!--@Html.DisplayNameFor(model => model.Department_Name)-->
            <p>Yksikkö</p>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Department_Name)
        </td>
        <td>
            @Html.ActionLink("Muokkaa", "Edit", new { id=item.Department_ID }) |
            @Html.ActionLink("Yksityiskohdat", "Details", new { id=item.Department_ID }) |
            @Html.ActionLink("Poista", "Delete", new { id=item.Department_ID })
        </td>
    </tr>
}

</table>
